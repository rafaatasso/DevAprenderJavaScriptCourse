// Basic types of data that redefine for writing programs:
// String, number, boolean, undefined and null

let name = 'Rafaela';       // string literal
let age = 23;               // number
let isApproved = true;      // boolean
let lastName;               // undefined
let selectedColor = null;   // null

console.log(name, age, isApproved, lastName, selectedColor);