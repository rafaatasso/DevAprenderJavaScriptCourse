// Array is a data set that can be accessed through an index
// An index is an exact position of an element

let family = [26, 45, 'Lexie', 50, 17, true, undefined];

console.log(family.length);
console.log(family);
console.log(family[2]);

/**************** HOME WORK ****************/

let colleaguesName = ['Sarah', 22, 'Laguna - SC'];
console.log(colleaguesName);